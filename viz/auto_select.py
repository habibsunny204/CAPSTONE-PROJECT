"""Result-shape -> chart-type rule table for AI-driven visualization (Task C3).

Not yet implemented. Design: keys off result DataFrame *shape* (datetime+numeric ->
line; categorical+numeric -> bar; numeric+numeric -> scatter; >=3 columns -> table) --
never off column identity, so this stays fully generic.
"""
