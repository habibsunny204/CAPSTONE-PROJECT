"""Data quality profiling and cleaning: missing %, duplicate count, IQR-based outlier
detection, and real cleaning steps (Task A3).

Not yet implemented. Design: profile_report(con, table_name) -> dict and
clean(con, table_name) -> dict, both pure functions with JSON-serializable output and
no Streamlit import, so Task C's UI can call them directly and render the results.
"""
