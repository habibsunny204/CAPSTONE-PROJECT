"""Performance benchmark proving filtered aggregation is <500ms on the full dataset,
with results logged as evidence to eval/results/ (Task A4).

Not yet implemented. Design: run groupby_agg(..., filters=[...]) scenarios from
configs/dataset_config.yaml against the full real dataset, report timing statistics,
and write a timestamped JSON evidence file.
"""
