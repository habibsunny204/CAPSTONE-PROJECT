[pytest]
# Tests marked live_llm hit real Gemini/Groq APIs -- they cost quota and depend on
# a provider being up, so they're excluded from the default run. Run them
# explicitly with: pytest -m live_llm
addopts = -m "not live_llm"
markers =
    live_llm: exercises live LLM provider APIs (excluded by default)
