# Copy this file to .env and fill in real values locally. Never commit .env itself.

# Task A1 — required once Customer.ID PII handling is resolved (see PROGRESS.md).
# PII_HASH_SALT=

# Task B — required once the LLM client is implemented.
# GEMINI_API_KEY=
# GROQ_API_KEY=
